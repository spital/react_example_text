# React example app

This small React example demonstrates hideable text and autocomplete inputs.
It uses Vite for development/builds and Vitest for the smoke test.

```bash
npm ci
npm start
```

Other useful commands are `npm run build`, `npm test`, and `npm run preview`.
